# Run 2026-07-11 — Redeploy (permanent fix test)

**IP:** `54.228.127.52`
**Prevention strategies verified:** All 6 fixes in place before deploy
**Manual steps after cloud-init:** Minimal (symlinks + compose file was clean)

---

## Results

### What worked (first time from cloud-init)
- `COMPOSE_PARALLEL_LIMIT=1` ✅ — no OOM during build
- MongoDB keyfile (`chown 999:999`, `chmod 400`, no newlines) ✅
- No duplicate VITE_* vars in compose (sed patch removed) ✅
- Admin Dockerfile `yarn vite build` (skips tsc type errors) ✅
- `default_server` in default.conf ✅
- Build context symlinks (`rxsoft-admin-3`, `healthcare-concepts`) ✅

### What still needed manual fix
- Postgres was in "Created" state (not started) after docker compose — had to `docker start` manually
- nginx subdomain config had to be deployed manually (dns-update.sh SCP hung)
- Admin JS patching still manual (VITE_* build args in Dockerfile didn't override built-in values)

### Challenges & Resolutions

#### C1: Postgres "Created" but not running
- **Symptom:** Backend couldn't resolve `postgres` hostname
- **Root cause:** Docker compose created the container but it never fully started
- **Resolution:** `sudo docker start rxsoft-postgres`
- **Prevention:** Add `--wait` flag or healthcheck wait in post-deploy script

#### C2: dns-update.sh SCP timeout
- **Symptom:** Script hung during scp, nginx configs not deployed
- **Root cause:** SSH connection issue with the new instance
- **Resolution:** Deployed configs manually with direct commands
- **Prevention:** Add SSH retry logic or connection timeout in script

#### C3: DDNS host typo in manual verification
- **Symptom:** All DDNS updates appeared to fail
- **Root cause:** Used `$host` (empty) instead of loop variable `$h`
- **Resolution:** Fixed the curl command
- **Prevention:** The script itself uses the correct `${host}` variable

### DNS Status
| Subdomain | DDNS | Namecheap A record |
|---|---|---|
| rxsoft (`@`) | ❌ | Needs to be created |
| api | ✅ | Exists |
| damorex | ✅ | Exists |
| apm | ✅ | Exists |
| conversation | ❌ | Needs to be created |

### Next Steps
1. Add `@` and `conversation` A + Dynamic DNS Records in Namecheap
2. Run `./dns-update.sh --env prod --skip-nginx` after adding records
3. Fix VITE_* build args to actually bake into admin JS (currently still needs post-build sed patch)
