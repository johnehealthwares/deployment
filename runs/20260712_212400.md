# Run 2026-07-12 — Destroy (aborted deploy)

**Action:** `terraform destroy`
**IP (before destroy):** `34.250.185.54`
**Instance:** `t3.small` (eu-west-1, 2GB RAM, 30GB gp3)
**Trigger:** User aborted during post-deploy build phase

---

## What Happened

### 1. Pre-Deploy Safeguard
- Read both run logs: `20260711_161000.md` and `20260711_162245.md`
- Validated all 6 documented fixes were in place **locally**:
  - ✅ `nginx-default.conf` has `listen 80 default_server;` (line 6)
  - ✅ `Dockerfile.rxsoft-admin` uses `yarn vite build` (line 18)
  - ✅ `docker-compose.prod.yml` has VITE_* in both `build:args:` and `environment:` — no duplicates
  - ✅ Post-deploy.sh handles MongoDB keyfile, swap, nest-cli.json, nginx context
  - ✅ `--profile` before `-f` in all commands

### 2. Terraform Drift Correction
- Detected security group drift (ingress rules added/removed outside Terraform)
- Ran `terraform apply -auto-approve` — only security group was modified (0 add, 1 change, 0 destroy)
- IP confirmed: `34.250.185.54`

### 3. SCP Files to Server
- 9 files copied (7 Dockerfiles + compose + nginx config)
- All verified landed on server

### 4. Server State
- Cloud-init had **failed** (same LIS type errors as prev runs — `npm run build` fails with 25 TS errors)
- No containers running
- Post-deploy script was not on the server (not committed to GitHub docker repo)

### 5. Post-Deploy Execution
- SCP'd `post-deploy.sh` to server
- Ran it — steps completed:
  - ✅ Compose file validated (no duplicate VITE_*)
  - ✅ nest-cli.json created for `healthcare-interoperability-switch` and `common-admin`
  - ✅ MongoDB keyfile fixed (`chown 999:999`, `chmod 400`)
  - ✅ Swap already existed (from cloud-init)
  - ✅ nginx config copied to admin build context
  - ✅ Databases started (postgres + mongodb)
  - 🚧 Backend build started (running `yarn install` + copying dist)
- **User aborted** during backend build

### 6. Destroy
- `terraform destroy -auto-approve` — 8 resources destroyed:
  - EC2 instance + EIP + SG + key pair
  - IAM role + profile + policy attachment
  - S3 bucket

---

## Key Observations

| Item | Status |
|---|---|
| Terraform state & drift detection | ✅ Working |
| Security group drift re-applied | ✅ |
| Cloud-init failure (LIS TS errors) | 🔁 Recurring — same as prev runs |
| Post-deploy.sh not on server | ❌ Need to push to GitHub docker repo |
| Backend build (yarn install) | ✅ Would have succeeded (dist pre-built in git) |

## Prevention Strategies for Next Run

1. **Push local docker files to GitHub** — `post-deploy.sh` should be in the GitHub docker repo so it's available on fresh instances
2. **Skip cloud-init service startup** — cloud-init tries to build all services and fails on LIS; only let it set up infra + clone repos
3. **Fix LIS TypeScript errors** — 25 errors in `rxsoft-lis-backend` block `npm run build`; either fix the errors or use pre-built dist
4. **Destroy `.ec2-ip` file** — stale IP (`34.250.185.54`) now points to destroyed instance
