# Namecheap DDNS password hash
# Get this from Namecheap → Domain List → ehealthwares.com → Advanced DNS
# Each A + Dynamic DNS Record entry shares the same domain-level hash.
# Copy this file to .env.ddns and fill in your hash.
DDNS_HASH=your_ddns_hash_here
